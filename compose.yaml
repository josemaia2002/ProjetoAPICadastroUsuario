services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=dbcadastrousuario'
      - 'POSTGRES_PASSWORD=password'
      - 'POSTGRES_USER=admin'
    ports:
      - '5432:5432'
